# v0.5.0 GitHub Pages / 설치형 PWA 수동 QA

현재 아래 **17개 판정 항목은 모두 NOT RUN**이다. 자동 Chrome/모바일 폭 검사를 실기기 PASS로 승계하지 않는다. 실제 결과를 사용자 보고와 함께 기록한다.

## 준비 및 기존 배포 QA (9개 항목)

운영 데이터를 삭제하지 않는다. v0.4에서 먼저 헤더 설정 → 백업 내보내기로 JSON을 내려받고 휴대폰 파일 앱에서 파일명/크기/내용을 확인한다. 이번 QA 데이터 이름은 모두 QA05로 시작한다. 가능하면 별도 브라우저의 테스트 환경에서 수행한다.

| 항목 | 버튼 순서와 예상/PASS 기준 | 상태 |
|---|---|---|
| APP-001 | v0.5 전체 소스를 실제 Pages에 반영 → HTTPS 저장소 하위 URL 열기 → 홈 v0.5.0 · DB 1 표시. 404/시작 오류 없음 | NOT RUN |
| APP-002 | Galaxy Chrome에서 페이지 열기 → Chrome 메뉴 → 앱 설치/홈 화면 추가. 아이콘 생성, 실행 가능 | NOT RUN |
| APP-003 | 홈 화면 아이콘으로 실행. 브라우저 주소창 없는 standalone과 하단 5탭 표시 | NOT RUN |
| APP-004 | 한 번 온라인 실행 후 비행기 모드(Wi-Fi도 OFF) → 앱 종료 → 아이콘 재실행. 홈/운동/설정 열림 | NOT RUN |
| CACHE-RUNTIME-001 | 기존 v0.4 설치 앱 온라인 실행 → 업데이트 안내의 업데이트 버튼 → 재실행. v0.5.0 표시, 기존 기록/메모/Template 유지 | NOT RUN |
| BACKUP-QA-EXPORT | 설정 → 백업 내보내기 → 파일 앱에서 JSON 보관. 파일이 존재하고 크기가 0보다 큼 | NOT RUN |
| BACKUP-QA-PREVIEW | 데이터가 있는 앱 설정 → 백업 복원 파일 선택 → 미리보기. Profile/건수 표시, 복원 버튼 비활성 | NOT RUN |
| BACKUP-QA-RESTORE | 데이터 없는 별도 브라우저에서 최초 앱 실행 → 설정 → 동일 JSON 선택 → 미리보기 → 복원. 성공/재시작, Profile·기록·삭제 상태 동일 | NOT RUN |
| BACKUP-QA-OFFLINE | 별도 pristine 환경에서 온라인 준비 후 비행기 모드 → 설정 내보내기·파일 선택·복원·재실행. 동일 기록과 정상 진단 | NOT RUN |

업데이트 안내가 안 뜨면 온라인 상태에서 앱을 닫고 다시 열고 잠시 기다린다. 사이트 데이터 삭제로 해결하지 않는다. 복원 환경은 처음 자동 생성된 Profile/필라테스/Template만 있어야 한다. 테스트 운동이나 이용권을 하나라도 만든 환경은 빈 환경이 아니다.

## 1. V05-QA-PASS — 60 → 59 → 59 → 60 → 59 → 60

1. 운동 → 운동 관리 → + 새 운동에서 이름 `QA05 필라테스`, 운동시간 필드를 선택 → 저장. 기존 기본 운동과 구분한다.
2. 운동 → 이용권 관리 → + 이용권. 운동 `QA05 필라테스`, 이름 `QA05 A 60회`, 총 횟수 `60`, 시작일 `2026-01-01`, 종료일 `2026-12-31`, 상태 활성 → 저장. 잔여 **60/60회**.
3. 운동 → + 운동 기록 → `QA05 필라테스`, 날짜 `2026-09-07 10:00`, 운동시간 `50`, 차감 이용권 `QA05 A 60회`, 메모 `QA05 LOG A` → 저장. 기록 상세에 **잔여 59회**.
4. 수정 → 메모만 `QA05 LOG A 수정` → 저장. **59회** 유지. 이용권 관리 → 사용내역에 사용 1건.
5. 기록 수정 → 차감 없음 → 저장. 이용권 관리 잔여 **60회**, 원장은 취소.
6. 같은 기록 수정 → A 선택 → 저장. **59회**, 사용내역은 기존 쌍 1건(재활성화).
7. 기록 상세 → 삭제 → 확인. 이용권 관리 **60회**.

PASS: 각 단계 숫자가 정확하며 동일 운동기록과 A 원장 ID가 백업 JSON에서 재적용 전후 동일하다. 원복: 기록을 삭제 상태로 두면 차감은 0. 사용이력 보존 때문에 A 삭제 대신 비활성화한다.

## 2. V05-QA-SCHEDULE — 예약 무차감, 완료와 되돌리기

1. A 활성/잔여60에서 캘린더 → + 예약 → 운동 QA05 필라테스, 예정 `2026-09-07 11:00`, 50분, 메모 `QA05 예약 A` → 저장. 잔여 **60**.
2. 캘린더 조회 시작 `2026-09-07`, 종료 `2026-09-07` → 조회 → 예약 열기 → 시간을 `11:30`으로 수정 → 저장. **60**.
3. 예약 열기 → 예약 취소 → 확인. 취소 표시, **60**. 다시 열기 → 상태 예정 → 저장.
4. 예약 열기 → 예약 완료 → 실제 운동일 `2026-09-07 11:30`, 50분, A 선택 → 저장 버튼을 빠르게 두 번 누른다. 예약 완료, 실제 운동기록 1건, **59**.
5. 완료 예약 열기 → 완료 취소 → 확인. 예정으로 돌아가고 연결 운동기록은 삭제, **60**.
6. 다시 예약 완료 → 같은 값과 A 선택 → 저장. **59**, 연결 기록 및 원장 ID는 최초 완료와 동일.
7. 비행기 모드 → 앱 완전 종료 → 재실행 → 동일 날짜 조회. 예약 완료, 실제 기록 1건, **59** 유지.

PASS: 위 숫자 및 상태 모두 일치. 캘린더의 완료 예약 1행과 실제 운동기록 1행은 서로 다른 원본 표시이며 중복 운동기록이 아니다. 원복: 예약 완료 취소 → 예약 취소, A 잔여60 확인.

## 3. V05-QA-VALIDITY — 실제 운동일 기준

1. A 활성 상태에서 운동기록 추가, 운동일 `2026-01-01 00:00`, 50분, A 선택 → 저장 성공(현재 날짜가 달라도 허용).
2. 수정 → `2025-12-31 23:59` → 저장. 유효기간 오류, 이전 기록과 잔여량 그대로.
3. `2026-12-31 23:59`로 수정 → 성공. `2027-01-01 00:00` 수정 → 거절.
4. 이용권 관리에서 다른 운동용 이용권을 추가해도 QA05 운동의 선택 목록에는 표시되지 않음.

PASS: 현지 날짜 양끝 포함, 범위 밖 저장 후 기존 기록/차감 변화 없음. 원복: 1번에서 만든 테스트 기록 삭제.

## 4. V05-QA-LIMIT — 사용량·비활성·복원 검증

1. A로 QA05 기록 2건 생성(2026-09-07 12:00/13:00, 각 50분). 잔여58.
2. 이용권 관리 → A 수정 → 총 횟수1 → 저장. 이미 사용한 횟수 오류, 총60/잔여58 유지.
3. 총60, 시작일2026-10-01로 저장 → 기존 운동일 제외 오류. 취소로 나온다.
4. A 비활성화. 기존 사용 기록의 메모 수정은 성공/잔여 동일. 한 기록 삭제 → 잔여59. 운동 → 삭제 기록 → 해당 기록 → 복원 → 비활성 이용권 오류, 삭제 상태 유지.
5. A 수정 → 활성 → 저장. 삭제 기록 복원 → 성공, 잔여58.
6. 별도 `QA05 B 1회`(같은 운동/기간) 생성. 기록1건 사용 후 삭제, 다른 기록이 B를 사용하여 잔여0으로 만든다. 삭제 기록 복원 → 잔여 부족 오류, 원장 추가 없음.

PASS: 한도·기간·비활성·소진 검증이 저장을 차단하고 기존 데이터 보존. 원복: 이 항목에서 만든 활성 기록 모두 삭제, A/B 사용원장 취소 확인.

## 5. V05-QA-CONFLICT — 두 화면 경쟁

1. 동일 Pages origin의 Galaxy Chrome 탭 2개에서 동일 예정 예약을 열고 예약 완료 입력 화면까지 이동한다. 서로 다른 브라우저는 DB를 공유하지 않으므로 이 테스트에 쓰지 않는다.
2. 두 화면에서 A/동일 실제 운동일을 선택 → 각 저장. 완료기록 1건/차감1회만 남아야 한다.
3. 완료 취소 후 오래된 완료 화면의 저장을 누르면 충돌 또는 최신 상태 안내이며 예정 상태를 임의로 다시 완료하지 않아야 한다.
4. 이용권 1회짜리에 두 탭에서 서로 다른 새 운동기록을 저장. 한 건 성공, 다른 건 잔여 부족, 부분 기록 없음.

PASS: DB 원본 확인은 설정 백업 내보내기 JSON의 연결 ID·status·행수를 비교한다. 원복: 완료 취소 후 예약 취소, 성공한 별도 기록 삭제.

## 6. V05-QA-HISTORY — 이용권 교체와 연결 보호

1. QA05 기록 한 건에 A 적용 → 수정에서 B 선택(잔여 확보) → 저장. A 복원/B 차감. 다시 A 선택 → 저장. A 차감/B 복원.
2. A/B의 사용내역에 이 기록당 각 1행, used는 합계1행. 같은 A 쌍의 ID는 최초와 동일.
3. 완료 예약의 운동기록 보기 → 삭제 → 확인. 예약 완료 취소를 먼저 하라는 안내와 기록/차감 유지.
4. 예약 완료 취소 후 운동 → 삭제 기록 → 연결 기록 복원 → 예약에서 다시 완료하라는 안내.

PASS: A↔B 교체와 보호 규칙 일치. 원복: 일반 기록 삭제, 연결 기록은 예약 완료 취소 후 예약 취소.

## 7. V05-QA-BACKUP — 새 업무 상태 보존

1. A 사용기록, B 취소 원장, 완료 취소된 예약 등 이력이 있는 상태에서 설정 → 백업 내보내기. 파일 별도 보관.
2. 새 브라우저의 pristine 앱 → 설정 → 파일 선택 → 미리보기 → 복원.
3. 운동/이용권/캘린더에서 잔여횟수·예약 상태·메모·삭제 기록·과거 양식 확인.
4. 복원 환경에서 백업을 다시 내보내기. 두 JSON의 integrity.payloadHash는 동일해야 한다(exportedAt은 달라도 됨).
5. 네트워크 OFF 재실행 후 다시 확인.

PASS: hash 및 화면 상태 동일. 원복: 원래 브라우저의 데이터는 변경하지 않는다. 시험용 환경은 시험 후 사용을 중지하며 운영 DB를 초기화하지 않는다.

## 8. V05-QA-REGRESSION — 기존 운동·양식·백업

1. 기존 v0.4 운동 기록의 날짜/메모/Template 버전을 확인하고 차감 없음으로 메모 수정. 기존 값 보존.
2. 테스트 운동의 운동 관리 → 해당 운동의 양식 → 필드 추가 → 저장. 과거 기록은 이전 버전, 새 기록은 새 버전.
3. 텍스트/숫자/선택/체크박스 필드를 각각 입력 후 저장·수정·삭제·복원. 잘못된 숫자 저장 거절.
4. 입력 중 뒤로/다른 탭 이동 시 미저장 확인. 저장 버튼 연속 터치 중 중복 기록 없음.
5. 설정 → 저장소 다시 진단: 정상, 14/14, DB1/Schema1/Seed1. 하단 5탭과 기존 백업 절차 유지.

PASS: 기존 기능 동작 유지와 사용자 데이터 손실 없음. 원복: QA05 테스트 기록 삭제, 테스트 운동 비활성화. 원장/soft-delete 이력은 정상적으로 남는다.

## 실패 시 기록사항 및 원복 공통

- 항목 ID, 실행 시각, 실제 URL, 앱 버전, Galaxy 모델/Android/Chrome 버전, 브라우저 탭 또는 standalone, 네트워크 상태.
- 누른 버튼 순서/입력값/변경 전후 총·사용·잔여량/예약 상태, 오류 문구 전체, 스크린샷.
- 가능하면 실패 직후 JSON 백업을 로컬에 보관하여 ID·revision·deleted_at·pass_id·completed_exercise_log_id·usage status 비교. 개인정보가 있는 백업을 공개 Git에 올리지 않는다.
- 실패를 해결하려고 사용내역 직접 수정, 사이트 데이터 삭제, 같은 저장 반복 실행을 하지 않는다. 새로 열어 실제 저장 상태를 먼저 확인한다.
- 테스트 중 강제 transaction 실패는 자동 테스트의 격리 DB에서만 수행한다. 운영 UI에는 실패 주입 버튼을 제공하지 않는다.
- 앱 코드 복귀 시 주의: v0.4는 이용권 원장 연동을 모르므로 v0.5에서 차감한 기록을 v0.4로 수정/삭제하면 정합성이 깨질 수 있다. 우선 v0.5 백업을 보관하고 쓰기를 중지한 채 수정 릴리스를 적용한다. DB downgrade/초기화는 필요하지 않다.

결과 보고 예: `V05-QA-PASS / PASS / Galaxy 모델 / Chrome 버전 / standalone / 60→59→59→60→59→60 확인 / 증적 파일명`.
