# v0.5.1 실제 GitHub Pages / Galaxy 설치형 PWA QA

이번 버전의 실기기 항목은 모두 **NOT RUN**이다. 자동 Chrome 검증과 구분한다. 기존 17개 배포/업무 회귀 항목에 아래 8개(QA051-MANUAL-01~08)를 추가하여 총 25개를 기록한다.

## 공통 사전조건 및 증적

실제 Pages HTTPS 하위 경로에서 홈 v0.5.1 / DB1 확인. Galaxy 모델·Android/Chrome 버전·실행 모드(탭/standalone)·URL·네트워크 상태를 기록한다. 초기화/강제 복원 테스트는 **운영 데이터와 분리한 브라우저**에서 수행한다. 기존 데이터는 설정 → 백업 내보내기로 보관하고, 새 브라우저에서 일반 복원하여 재현용 데이터로 만든다. 초기화/교체는 모든 Profile의 portable 기록을 대상으로 하며 device_id와 기기 설정은 유지한다.

테스트 실패 시 공통 기록: Test ID, 정확한 버튼 순서/입력값, 실패 직전/직후 화면, 오류 메시지/코드, 잔여횟수·예약상태, 앱 버전과 시각. 가능하면 전후 JSON을 로컬에 보관하고 ID/revision/deleted_at 및 integrity.payloadHash를 비교한다. 실제 건강 데이터 백업을 공개 저장소에 올리지 않는다.

## QA051-MANUAL-01 — 예약 기본 이용권 (자동 매핑 QA051-01/02)

- 사전조건: 운동 → 운동 관리 → + 새 운동 → `QA051 필라테스`, 운동시간 필드 → 저장.
- 순서/값: 운동 → 이용권 관리 → + 이용권 → QA051 필라테스 / `QA051 만료` / 60 / 2000-01-01~2000-12-31 / 활성 저장. 같은 운동의 `QA051 A` / 60 / 2026-01-01~2026-12-31 저장. 이어 `QA051 B`를 동일 조건으로 저장.
- 캘린더 → + 예약 → QA051 필라테스 / 2026-09-07 10:00 / 50분 저장. 2026년 9월로 이동 → 7일 → 예약 열기 → 예약 완료.
- 예상: 만료권은 후보에서 제외되고 생성순 첫 사용 가능권 A가 기본 선택. `차감 없음` 또는 B로 직접 변경 가능. A 선택 저장 시 잔여59, 차감 없음이면60.
- 이용권 관리에서 A/B 비활성화 후 별도 예정 예약 완료 화면을 열면 차감 없음이 기본값.
- PASS: 만료/비활성/잔여0 후보 기본 선택 없음, 유효 첫 항목 선택과 사용자 재선택 정상. 날짜 변경 후 최종 저장 검증은 실제 운동일 기준 유지.
- 원복: 완료 예약은 완료 취소 후 예약 취소. A/B 활성으로 돌리거나 시험용 비활성 상태로 둔다.
- 상태: NOT RUN. 실패 정보는 공통 증적 + 옵션 표시 순서/선택값/운동일.

## QA051-MANUAL-02 — 월간 달력 및 통합 표시 (QA051-03~06)

- 사전조건: 오늘 날짜의 예정 예약1건, 완료 예약1건, 직접 운동기록1건. 세 항목의 메모를 `예정/연결/직접`으로 구분한다.
- 순서: 캘린더 첫 진입 → 오늘이 선택된 월간 달력 확인 → 다른 날짜 클릭 → 이전 달 → 다음 달 → 오늘 버튼.
- 예상: 오늘 밑줄/테두리와 선택일 채움이 구분됨. 누른 날짜 데이터만 하단 표시. 완료 예약과 연결 기록은 `QA051 필라테스 · 완료 · 운동기록` 한 항목에 예약 열기/기록 열기 버튼. 직접 기록은 별도1건. 원본 데이터를 합치거나 삭제하지 않음.
- 경계값: 예정2026-08-31 10:00인 예약을 실제2026-09-01 10:00으로 완료. 8월31일에 완료 항목이 남지 않고 9월1일에 단일 항목 표시.
- PASS: 기본오늘/월이동/날짜필터/연결중복제거/직접기록 분리 모두 일치, 가로 넘침 없음.
- 원복: 시험 완료는 완료 취소, 예약 취소; 직접 기록 삭제.
- 상태: NOT RUN. 실패 정보: 표시월/선택일/실제일·예정일, 중복 항목 수와 연결 UUID.

## QA051-MANUAL-03 — 운동 종류 변경 중 입력 보존 (QA051-07~09)

- 사전조건: `QA051 운동A`는 운동시간/거리, `QA051 운동B`는 운동시간만 갖도록 운동 관리 → 양식에서 설정.
- 순서: 운동 → + 운동 기록 → A 선택 → 날짜2026-09-07 09:23 / 운동시간37 / 거리5 / 메모`전환 유지` 입력 → B 선택.
- 예상: 날짜/시간/메모/운동시간37 유지, 거리 입력 제거. A로 다시 바꿔도 이전에 제거된 거리값5가 부활하지 않음.
- 추가: 빠르게 A→B→A 전환 후 마지막 운동의 양식만 남고 저장 중 양식 혼용 없음. 같은 key지만 type 변경/허용 옵션 변경은 자동 테스트 fixture로 검증하며 실기기에서는 가능한 양식 변경 범위에서 확인한다.
- PASS: 공통 입력과 호환키 보존, 전용/비호환 값은 저장에 섞이지 않음.
- 원복: 입력 취소 → 미저장 이동 확인. 저장했다면 시험 기록 삭제.
- 상태: NOT RUN. 실패 정보: 전환 전후 field key/type 및 입력 스크린샷.

## QA051-MANUAL-04 — 상태 버튼과 원장 (QA051-10~12)

- 사전조건: A 이용권 사용1회, 총60/잔여59. 초기 JSON으로 pass revision과 usage 행을 기록.
- 순서: 운동 → 이용권 관리 → A의 비활성화 → 화면 활성화 버튼 확인 → 활성화.
- 예상: active에서 수정|비활성화, inactive에서 수정|활성화. 잔여59/사용1 유지, usage 행 내용 동일. pass revision만 각1 증가. deleted_at=null 유지.
- PASS: 버튼/상태/원장·잔여/ revision 규칙 모두 일치.
- 원복: 최초 상태로 돌리고 시험 기록 삭제(잔여60).
- 상태: NOT RUN. 실패 정보: 전후 상태/잔여량과 pass·usage revision.

## QA051-MANUAL-05 — 백업 후 전체 초기화 (QA051-13/14/16/22)

- 사전조건: 시험 브라우저에 기록·이용권·예약·삭제 이력. 기기 식별값은 원격 개발자 도구 사용 가능 시 device_settings.device_id를 읽기만 하여 기록.
- 순서: 설정 → 전체 데이터 초기화 → `초기화 전에 현재 데이터를 백업하시겠습니까?` → 예 · 백업 후 초기화.
- 예상1: JSON이 다운로드됨. **아직 데이터는 지워지지 않음**. 다운로드 파일 확인 입력 표시.
- 파일 앱에서 방금 내려받은 JSON 존재 확인 → `저장된 백업 파일 확인 후 실행`에서 그 파일 선택.
- 예상2: validator/hash 일치 후에만 초기화 → 앱 재실행. 기본 내 프로필/필라테스/Template1만 있고 운동기록·이용권·예약0. 설정 진단 정상/14 Store/DB1/Schema1/Seed1. device_id 동일.
- 실패 분기: 다운로드 취소 또는 파일 선택 취소 → 초기화 없음. 다른 정상 백업/손상 파일 선택 → 오류 및 초기화 없음. 백업 생성 실패(사진 미지원 등)는 초기화 금지. 검증 오류 뒤 취소 버튼으로 복귀.
- PASS: 백업 성공과 파일 재검증 전에는 삭제0, 성공 후 완전한 초기 상태. 자동 강제오류는 격리 테스트에만 주입한다.
- 원복: 초기화된 pristine 앱의 설정 → 백업 복원 → 저장 파일 선택 → 복원. 원본 hash/기록 확인.
- 상태: NOT RUN. 실패 정보: 다운로드 파일명/크기/hash, 파일선택 시각, 전후 건수/기기 ID.

## QA051-MANUAL-06 — 백업 없는 초기화 (QA051-15/16/22)

- 사전조건: 되돌릴 별도 JSON이 이미 있는 시험 브라우저. 운영 환경에서 수행하지 않음.
- 순서: 설정 → 전체 데이터 초기화 → 아니오 · 백업 없이 초기화 → 최종 확인에서 먼저 취소.
- 예상1: 기록 그대로. 다시 동일 순서로 최종 확인 승인.
- 예상2: 별도 다운로드 없이 초기 상태 생성, device_id 유지. 여러 Profile 데이터도 portable 범위에서 제거됨.
- PASS: 취소 시 변화0, 승인 시 부분 Seed/고아 기록 없이 정상 초기 상태.
- 원복: 사전에 보관한 백업으로 일반 복원.
- 상태: NOT RUN. 실패 정보: 최종 확인 승인 여부/전후 Profile·Store 건수.

## QA051-MANUAL-07 — non-pristine 강제 복원 (QA051-17~22)

- 사전조건: A 브라우저 백업 `원본.json`. B 시험 브라우저에 `교체될 기록`을 새로 만들어 non-pristine으로 준비.
- 순서: B 설정 → 백업 복원 → 원본.json → 미리보기. 일반 복원 비활성, 강제 복원 버튼 표시 확인.
- 강제 복원 → `현재 기록이 모두 백업 파일의 내용으로 교체됩니다.` 확인 → `현재 데이터를 먼저 백업하고 강제 복원` → 내려받은 B JSON을 재선택.
- 예상: B의 교체될 기록이 없어지고 원본 A의 UUID/profile_id/revision/created_at/updated_at/deleted_at/관계 그대로 복원. B device_id는 유지. B에서 다시 export한 integrity.payloadHash는 A 원본과 같음.
- 반복 시험: 시험용 기록 추가 후 파일 다시 선택 → 강제 복원 → 백업 없이 강제 복원 → 최종 승인. 결과 동일.
- 안전 분기: 준비/백업 확인 중 다른 탭에서 새 기록 저장 후 실행하면 대상 변경 오류로 중단. 다시 미리보고 새 백업을 만든 뒤 진행. 손상 checksum/schema/reference/unique 파일은 미리보기 단계에서 거절되며 강제 버튼 없음.
- PASS: 일반 차단 유지, 명시적 replace만 성공, merge 없음, 실패 시 기존 데이터 유지, hash/기기 ID 조건 충족. 중간 transaction 강제오류는 자동 검증에 한정.
- 원복: 백업한 B JSON을 강제 복원하여 B의 원상태로 복귀(일반 복원은 non-pristine 차단).
- 상태: NOT RUN. 실패 정보: 원본/보호 백업 hash·Profile UUID, 잔존·누락 ID, 오류 코드.

## QA051-MANUAL-08 — 오프라인·업데이트·기존 전체 회귀 (QA051-23/24)

- 사전조건: 실제 Pages 온라인 1회 실행/캐시 준비, v0.5.0에서 업데이트하여 v0.5.1 표시 확인, 보호 JSON 보관.
- 순서: Galaxy 비행기 모드 + Wi-Fi OFF → 설치형 앱 완전 종료 → 재실행. 월간 달력/기록 입력/이용권 상태 변경 실행. 시험 환경에서 05~07 백업·초기화·강제복원 흐름 반복.
- 예상: 오프라인 다운로드·파일 재검증·교체 및 재실행 성공. 기존 기록/Template/메모/soft-delete/revision 보존.
- 기존 차감 회귀: 60→운동1회59→메모수정59→차감해제60→재적용59→삭제60. 예약 생성/수정/취소60→완료59→중복완료59→완료취소60→재완료59.
- 기존 backup 일반 경로: 새 pristine 환경 정상 복원, populated 환경 일반 복원 차단. 삭제 기록 복원에서 inactive/기간/잔여량 재검증. 기존 5탭/진단/Template 버전 증가 유지.
- PASS: 모든 흐름 일치, 오프라인 재실행 후 상태 동일, 실제 사용자 기록 손실 없음. 사용자 실행한 범위만 PASS로 보고.
- 원복: 시험 완료 취소/예약 취소/일반 시험 기록 삭제, 또는 보호 JSON으로 강제 복원. 초기화/교체는 되돌리기 버튼이 없으므로 반드시 보관한 JSON을 사용한다. DB 자체 삭제로 문제를 해결하지 않는다.
- 상태: NOT RUN. 실패 정보: 네트워크/standalone 여부, 업데이트 전후버전, 실패 단계와 보호파일 존재.

## 기존 17개 수동 판정 항목

APP-001/002/003/004, CACHE-RUNTIME-001, BACKUP-QA-EXPORT/PREVIEW/RESTORE/OFFLINE, V05-QA-PASS/SCHEDULE/VALIDITY/LIMIT/CONFLICT/HISTORY/BACKUP/REGRESSION을 이번 버전에서도 NOT RUN으로 유지한다. 위 시나리오의 대응 부분과 기존 REGRESSION_TEST.md 기준으로 실제 재실행 후 개별 보고한다. 예전 PASS나 자동 테스트 결과로 승계하지 않는다.

보고 예: `QA051-MANUAL-07 / PASS / Galaxy 모델·Chrome / standalone / 보호파일 재선택 후 교체 / 원본 hash 일치 / 증적 파일명`.
