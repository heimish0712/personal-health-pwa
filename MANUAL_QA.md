# v0.8.0 Dashboard + Unified Calendar 사용자 QA

**DASH-MANUAL-01~07: NOT RUN.** 기존 실기기 회귀 41건도 v0.8에서 미실행이다. 자동 Chrome 실행은 실제 Galaxy/설치형 PWA의 PASS로 계산하지 않는다. 최신 기대값: APP/cache 0.8.0 / DB2 / Schema1 / Seed1 / 15 Store. 아래 과거 절차의 버전 기대값은 이번 실행에서는 이 값으로 읽는다.

## 공통 사전조건·기록·원복

- 운영 데이터와 분리한 시험용 브라우저/설치형 PWA에서 실행한다. 먼저 기존 앱 설정 → 백업 내보내기로 실제 JSON/ZIP 파일을 보관하고 설정 → 파일 선택으로 검증 성공을 확인한다. 이번 버전은 DB Migration이 없다.
- 빈 시험 Profile 기준 수치다. 기존 데이터가 있으면 시작 전 홈 수치와 해당 날짜 원본을 기록하고 아래 증가분으로 비교한다. D=시험 당일(예: 2026-09-07), P=달력에서 선택한 이전 날짜(예: 2026-09-03), Profile timezone=Asia/Seoul. 월~일이 이번 주다.
- 각 Test ID마다 실행시각, Pages 주소, Galaxy/Android/Chrome 버전, standalone 여부, APP/DB/Schema, 온라인/오프라인 상태, 실제값, PASS/FAIL/NOT RUN을 기록한다.
- 실패 시 마지막 버튼/입력값, 예상/실제 화면, 선택 날짜/시간대, 콘솔 오류 또는 공개 오류코드, 전후 백업을 로컬에 보관한다. 숫자 오류는 해당 원본 ID/revision/deleted_at/연결 ID도 백업에서 확인한다. 건강정보 백업을 공개 저장소에 올리지 않는다.
- 원복은 만든 시험 기록만 각 원본 화면에서 삭제하고 예약은 완료취소 후 예정 취소, 이용권은 비활성화한다. soft-delete 이력/사진은 보존된다. 정확한 원상복구가 필요하면 보관 파일을 먼저 검증하고 시험 환경에서만 기존 명시적 강제 복원 절차를 사용한다. 운영 DB/브라우저 데이터를 오류 해결 목적으로 초기화하지 않는다.

## DASH-MANUAL-01 — 홈 운동·이용권 숫자

- 목적/사전조건: 실제 운동과 원장 집계 확인. 공통 준비 완료, 오늘/이번 주 운동 없는 시험 Profile.
- 순서/값: 운동 → 이용권 관리 → 추가 → 필라테스, 이름 `대시보드 QA 60`, 총60, 시작 D, 종료 D의 다음 달 같은 날, 활성 → 저장. 홈 → 빠른 기록 운동 → 날짜 D 10:00, 시간30분, 해당 이용권 선택 → 저장 → 홈.
- 단계별 예상: 이용권 추가 직후 잔여60. 실제 운동 저장 후 이번 주1회/30분, 최근 운동 필라테스, 잔여59. 운동기록 상세 → 수정 → 메모 `홈 집계 QA`만 저장 → 홈에서도1회/30분/59.
- PASS: 위 숫자가 정확하고 해당 최근 운동/이용권 버튼이 원본을 연다. 기록 삭제 → 홈0회/0분/60, 삭제 포함에서 복원 →1회/30분/59까지 확인.
- 실패 기록: 운동일, duration_minutes, 원장 used_count/status, 전후 숫자.
- 원복: 다음02에 이 이용권을 사용하고 마지막에 해당 운동 삭제/이용권 비활성화.

## DASH-MANUAL-02 — 완료 예약 한 사건 / 완료취소

- 목적/사전조건: 01 완료 상태(1회/30분/잔여59).
- 순서/값: 캘린더 → 오늘 → +예약 → D 14:00, 필라테스50분 → 저장. 홈의 오늘 예정에 표시되고 잔여59 유지. 캘린더 해당 예약 열기 → 예약 완료 →50분/같은 이용권 → 저장 → D 선택.
- 예상: `필라테스 · 완료 · 운동기록` 하나에 예약 열기/기록 열기가 함께 보인다. 01의 직접 운동은 별도로 표시. 홈2회/80분/잔여58. 완료 예약 열기 → 완료취소 → 확인 → 오늘: 예정 사건 하나, 홈1회/30분/잔여59.
- PASS: 완료 상태에 연결 운동이 별도 중복 카드로 나타나지 않고 취소 후 즉시 값이 바뀐다. 다시 완료해도 해당 사건 하나, 잔여58.
- 실패 기록: 예약일/실제 운동일, 두 원본 연결 ID, 표시 카드 수.
- 원복: 완료취소 → 예약취소, 01 운동 삭제 후 이용권 비활성화.

## DASH-MANUAL-03 — 체중·인바디 관계

- 목적/사전조건: 최근 측정/변화량/관계 확인. 더 미래의 측정이 없는 시험 Profile.
- 순서/값: 홈 → 체중 → D 08:00/70kg → 저장. 홈 → 인바디 → D 09:00/69.5kg/골격근량30/체지방률24/체중에도 추가 ON → 저장 → 홈 → 캘린더 D.
- 예상: 최근69.5kg, 직전 대비-0.5kg, 최근 인바디30kg/24%. 캘린더에는 일반 체중1건과 `인바디 · 체중 연동`1건, 해당 날짜에 체·인 indicator. 연동 항목 열기는 인바디 상세로 이동.
- PASS: 인바디 연동 체중이 세 번째 측정 카드로 중복되지 않는다. 인바디 수정69kg → 홈69kg/-1kg 및 캘린더69kg로 갱신.
- 실패 기록: 두 measured_at, source/source_ref_id, 원본 값과 표시값.
- 원복: 인바디 원본 삭제(연동 체중 함께 삭제), 일반 체중 삭제.

## DASH-MANUAL-04 — 식단 수·thumbnail

- 목적/사전조건: 오늘 집계와 실제 사진 표시. 오늘 식단 없는 시험 Profile.
- 순서/값: 홈 → 식단 → D 08:30/아침/`달걀 2개` → 저장. 홈 → 식단 → D 12:30/점심/`밥과 샐러드` → 사진2장 선택 → 저장 → 홈.
- 예상: 오늘2건, 최근 `밥과 샐러드`, thumbnail1장. 식단 열기로 들어가면 원본 사진2장. 캘린더 D에는 식단2건과 식 indicator. 점심 수정에서 날짜를 D 다음 날로 옮기면 오늘1건이며 캘린더 날짜도 이동.
- PASS: 홈은 thumbnail 하나만 표시하고 올바른 원본을 연다. 텍스트/사진/날짜 재조회가 모두 일치한다.
- 실패 기록: 사진 순서, 최근 식단 eaten_at, 캘린더 선택 날짜, 이미지 로딩 여부.
- 원복: 두 식단 삭제. 사진은 복원용 tombstone 참조로 남는 것이 정상이다.

## DASH-MANUAL-05 — 날짜별 빠른 기록·뒤로가기

- 목적/사전조건: 기존 폼의 선택 날짜 전달과 dirty guard. 공통 준비 완료.
- 순서/값: 캘린더 이전 달/다음 달 왕복 → P 선택 → 선택 날짜에 기록 체중 → 날짜 P 기본값 확인 →66.2kg 저장. 같은 방식으로 인바디66.1kg(연동 OFF), 식단 `날짜 QA`, 운동30분(차감 없음) 각각 추가.
- 예상: 네 폼 모두 P 날짜 기본값. 저장 후 P가 선택된 캘린더로 돌아오고 각 기록이 보임. 운·식·체·인 indicator가 나타남. 다시 체중 입력 →65kg만 입력 → 취소/브라우저 뒤로 → 이탈 거절 시 입력 유지, 이탈 승인 시 저장 없이 캘린더로 복귀. 달력 재실행 시 hash의 선택 날짜 유지, 하단 캘린더 탭 새 진입은 오늘 선택.
- PASS: 날짜/분류/건수 일치, 입력 폼 복제 없이 기존 화면과 동작 동일, 미저장 이탈 확인 정상. 오늘 버튼은 실제 오늘을 선택한다.
- 실패 기록: 주소 hash, 선택 날짜, 폼 날짜, 돌아온 날짜, dirty 확인 선택값.
- 원복: P에서 생성한 네 원본 기록만 삭제.

## DASH-MANUAL-06 — 설치형 PWA 오프라인

- 목적/사전조건: 03~05 중 일부 시험 기록이 남고 온라인에서 v0.8을 한번 연 상태.
- 순서: 홈 숫자/thumbnail과 캘린더 P 화면 기록 → 비행기모드(Wi-Fi도 OFF) → 앱 완전히 닫기 → 설치 아이콘으로 재실행 → 홈/캘린더 월 이동/P 선택 → P 체중66kg 추가 → 앱 다시 종료/재실행.
- 예상: 기존 숫자/사진/날짜 목록 유지. 외부 네트워크 없이 빠른 기록 저장/표시 및 재실행 후 유지.
- PASS: 로딩 실패/사진 누락/중복이 없고 전후 원본과 표시가 일치한다.
- 실패 기록: 앱 종료 방식, offline 상태, cache/app version, 마지막 성공 화면과 오류.
- 원복: 네트워크 복구 후 이 테스트에서 추가한 체중만 삭제.

## DASH-MANUAL-07 — 백업 복원 후 동일 표시

- 목적/사전조건: 시험 데이터(운동/완료예약/이용권/체중/연동인바디/사진식단)가 있는 원본과 별도 pristine 브라우저. 01~04 원복 전에 수행하거나 같은 값을 다시 만든다.
- 순서: 홈 숫자 및 캘린더 D/P 목록·사진 기록 → 설정 → 백업 내보내기 → 실제 ZIP 파일 보관 → 별도 초기 브라우저에서 같은 v0.8 Pages → 설정 → 파일 선택 → 검증/미리보기 → 일반 복원 → 재실행 → 홈/동일 날짜 확인.
- 예상: UUID/revision/관계가 보존되고 홈 숫자/최근값/thumbnail/캘린더 사건 수와 완료·연동 관계 모두 원본과 같다. 사진 없는 JSON v1 파일도 같은 방식으로 별도 pristine 환경에서 복원 가능하다.
- PASS: 동일 Profile timezone/동일 시험 날짜 기준으로 모든 표시가 같다. 자정 또는 주 변경을 넘겨 실행했다면 오늘/이번 주 집계 범위가 바뀐 사실을 구분해 재조회한다.
- 실패 기록: 파일 크기·backupVersion, 검증 오류, 원본/대상 App/DB, 두 표시 화면/원본 ID.
- 원복: 원본은 변경되지 않는다. 별도 시험 대상은 유지하거나 검증된 사전 백업을 명시적 복원한다. 운영 데이터 병합은 실행하지 않는다.

---

# v0.7.0 식단·사진·ZIP Backup 사용자 QA

DIET-MANUAL-01~08은 **NOT RUN**. 기존33건 역시 새 버전 실기기 PASS로 자동 승계하지 않는다. 설치형 PWA 실제 검증과 자동 Chrome 결과를 구분한다. 이전 버전 절차는 이 문서 하단에 보존했다. 재실행 시 최신 기대값은 APP0.7.0 / DB2 / Schema1 / Seed1 / 15 Store다.

## 공통 준비 / 실패 증적 / 원복

- 업데이트 **전에** v0.6 앱 설정 → 백업 내보내기 → 실제 JSON 파일이 다운로드됐는지 확인하고 보관한다. 일상 데이터와 분리한 시험 브라우저/시험용 설치 앱을 우선 사용한다. 사진이 생긴 후 백업은 ZIP이다.
- Pages 주소, Galaxy 모델, Android/Chrome 버전, standalone 여부, 앱 버전/DB/Schema, 네트워크 상태, 실행시각과 Test ID를 기록한다. D=시험 당일 날짜(예: 2026-09-07), 시간은 Asia/Seoul 기준.
- 실패 시 정확한 마지막 버튼/입력값, 원본 사진 형식·크기·방향, 전후 화면/오류코드, 설정 저장공간, 기록 UUID/revision/deleted_at/storage_key/checksum과 전후 백업 파일을 **로컬에** 보관한다. 실제 건강정보 백업을 공개 저장소에 올리지 않는다.
- 식단 기록 삭제는 soft-delete이며 사진 파일도 복원용으로 남는다. 사진을 먼저 개별 제거한 후 식단 삭제/복원을 하더라도 개별 제거 사진은 되살아나지 않는다. 고아 정리는 참조가 없는 파일만 삭제하므로 tombstone 사진 용량은 줄지 않는 것이 정상이다.
- 시험 원복은 시험 식단만 삭제한다. 사진을 포함해 완전히 지우려면 **분리한 시험 환경에서만** 설정의 명시적 전체 초기화를 사용한다. 기존 DB를 삭제하거나 앱 데이터를 지우는 방식으로 Migration 오류를 해결하지 않는다.

## DIET-MANUAL-01 — 업데이트 / Migration 기존 데이터 보존

- 목적/사전조건: v0.6에서 운동/Template v1-v2/이용권/예약/일반 체중/연동 인바디와 삭제된 기록을 보유한 시험 환경, 업데이트 전 JSON 보관. 가능하면 새 브라우저 v0.6에 해당 백업 복원 후 시험한다.
- 순서: v0.6 홈 버전/DB1과 이용권 잔여·체중 최근값·운동 목록을 기록 → Pages를 v0.7로 적용 → 업데이트 안내의 업데이트 실행 또는 다시 열기 → 홈 v0.7.0 / DB2 → 설정 Schema1/15 Store/정상 확인.
- 업데이트 후 설정 → 백업 내보내기. 사진이 아직 없으므로 JSON v1이 정상이다. 전후 JSON의 `data`를 비교해 모든 UUID/revision/관계/삭제 상태가 일치하고 integrity.payloadHash가 같아야 한다. source.appVersion/dbVersion/exportedAt의 변경은 정상이다.
- 기존 운동·이용권 잔여·예약·체중·인바디 화면을 열어 값/연결 유지 확인.
- PASS: 기존 portable100% 보존, media_blobs만 추가, 빈 Profile/운동 재생성0. 실패 증적: 전후 JSON·앱/DB 화면·blocked/migration 오류.
- 원복: 운영 DB 유지. 이전 앱은 DB2를 열 수 없으므로 파일만 v0.6으로 되돌리지 않는다. 필요하면 별도 환경의 v0.6에 **업데이트 전 JSON**을 복원해 원본을 확인하고 문제를 보고한다.
- 상태: NOT RUN.

## DIET-MANUAL-02 — 실제 카메라 / 방향 / 압축본

- 목적/사전조건: Galaxy 설치형 앱, 카메라 사용 가능한 환경. 개인식별정보 없는 시험 음식/색종이를 준비한다.
- 순서: 식단 → + 식단 기록 → D 12:10 / 점심 / 내용 `QA070 밥·계란·채소` / 메모 `카메라 세로` → 촬영 → 세로 방향 사진 촬영 → 사용/확인.
- 예상: 처리 안내 후 미리보기1장. 저장 전 식단 목록에는 아직 생기지 않는다. 저장 → 상세에 정방향 사진과 입력값 표시. 식단 목록은 작은 thumbnail 표시.
- 같은 방법으로 가로 사진도 별도 식단에 저장한다. 설정 → 백업 내보내기 → ZIP 내 data.json에서 압축본 긴변≤1280, thumbnail긴변≤320, MIME WebP 또는 JPEG, byte_size/checksum 확인. 원본 카메라 파일은 ZIP/DB에 별도로 없어야 한다.
- PASS: 세로/가로 방향 정상, 글자/음식이 찌그러지지 않음, 원본 대신 압축본2종 생성, 저장1회에 식단1건.
- 실패 증적: 카메라 원본 방향/형식/크기, 미리보기/상세, metadata 크기. 원복: 시험 식단 삭제.
- 상태: NOT RUN. 자동 합성 EXIF 테스트는 카메라 기기 검증을 대신하지 않는다.

## DIET-MANUAL-03 — 갤러리 여러 사진 / 혼합 수정 / 개별 제거

- 목적/사전조건: 구별되는 JPEG/PNG/WebP 사진 A/B/C를 준비. 장당25MB 이하, 전체 선택12장 이하.
- 순서: + 식단 기록 → D 15:00 / 간식 / `QA070 과일` / `갤러리 A B` → 사진 선택 → A와B 선택 → 2장 미리보기 → 저장.
- 식단 열기 → 수정 → A의 사진 제거 → 사진 선택 → C 추가 → 메모 `B C 유지` → 저장. 예상: 기존 B 다음 새 C 순서, B UUID/storage_key 유지, C 신규, A tombstone.
- 같은 D 15:10/간식으로 사진 없는 기록 `QA070 차`도 저장. 같은 날 같은 식사구분2건 허용.
- 불량/지원되지 않는 파일(HEIC 또는 사진이 아닌 파일)을 선택하거나 12장을 초과한다. 저장되지 않고 형식/장수 안내, 기존 식단과 사진은 유지되어야 한다. 잘못된 선택은 취소한다.
- PASS: 기존+신규2장 순서 안정, 개별 제거/메모 반영, 같은 구분 여러 기록, 실패로 DB 부분 변경0.
- 실패 증적: A/B/C 구분·sort_order·UUID와 입력파일 수/형식. 원복: 시험 식단 삭제.
- 상태: NOT RUN.

## DIET-MANUAL-04 — 삭제/복원 / 저장공간 / GC

- 목적/사전조건: MANUAL-03의 B/C 식단과 제거된 A tombstone 유지.
- 순서: 식단 열기 → 삭제 → 확인. 일반 날짜 목록에서 사라지고 삭제된 기록 포함에서 보인다. 식단 열기 → 복원. B/C만 다시 표시, A는 복귀하지 않는다.
- 설정 → 사진 저장공간의 전체 사용량/한도, 사진 사용량/파일 수 확인 → 영구 저장 요청. 브라우저의 허용/거절 결과가 표시돼야 한다(거절 자체는 오류가 아니다).
- 고아 사진 파일 정리 실행. 기존 B/C와 tombstone A는 **정리 대상이 아니며** 사진/복원은 그대로 동작한다. 고아0이면 변화없음 정상. 실제 공간 부족을 강제로 만들기 위해 운영 저장소를 채우지 않는다.
- PASS: 삭제/복원 의미 유지, 사진 사용량 확인, 참조 사진 손실0. 저장공간 부족을 실제로 만났다면 안전한 안내와 기존 값 보존 후 장수/용량 축소 재시도 가능.
- 실패 증적: 정리 전후 사용량/고아 수·사진 상태·오류문구. 원복: 시험 기록 삭제, 완전 공간회수는 분리한 시험 환경에서만 전체 초기화.
- 상태: NOT RUN.

## DIET-MANUAL-05 — 완전 오프라인 사진 CRUD / 재실행 / Calendar

- 목적/사전조건: 온라인에서 v0.7 앱과 식단/설정을 한 번 열어 shell을 준비, 갤러리 파일을 기기에 보관.
- 순서: 비행기 모드 ON, Wi-Fi OFF → 앱 완전 종료 → 설치 아이콘으로 재실행 → 식단. 기존 thumbnail/상세사진 정상 표시.
- + 식단 기록 → D 18:30 / 저녁 / `QA070 오프라인` / 메모 `재실행 확인` → 갤러리 사진2장 → 저장. 수정에서 한 장 제거/메모 변경 → 저장 → 삭제 → 복원.
- 캘린더 → 오늘 → 식단 · 저녁 항목 → 식단 열기 → 해당 식단 상세. 사진은 상세에서 확인한다. 앱 종료→재실행 후 동일사진/값/순서 보존.
- PASS: 네트워크 없이 전체 흐름, 목록thumbnail/상세압축본, Calendar원본이동, 재실행 후 동일 상태.
- 실패 증적: 네트워크 설정·화면·사진 파일명/건수/메모·시각. 원복: 네트워크 복구 후 시험 식단 삭제.
- 상태: NOT RUN.

## DIET-MANUAL-06 — Backup v2 PC → Galaxy 새 환경 복원

- 목적/사전조건: PC 시험 앱에서 식단 사진2장 이상과 삭제한 사진 이력, 운동/체중 기록을 준비. Galaxy 대상은 기존 데이터가 없는 별도 브라우저/설치 환경. 기존 폰 데이터를 초기화하지 않는다.
- 순서: PC 설정 → 백업 내보내기 → 다운로드 폴더에서 `personal-health-backup-v2-....zip` 실제 파일 확인. ZIP을 USB 등으로 폰에 **그대로** 복사(압축 해제/재압축 금지).
- 폰 설정 → 백업 복원 → ZIP 선택 → 미리보기 Profile/기록/사진 건수·무결성 확인 → 일반 복원. 대상은 pristine이어야 하며 기존 데이터가 있으면 버튼 차단이 정상이다.
- 복원 후 식단 목록thumbnail/상세사진·삭제 상태·메모·순서, 기존 운동/체중을 확인. 폰에서 다시 백업 ZIP을 내보내 PC로 복사한다.
- 두 ZIP data.json의 portable integrity.payloadHash와 mediaManifest(storage_key/created_at/byte_size/checksum/MIME)가 동일해야 한다. export시각과 source 정보 차이는 허용. 폰 비행기 모드 재실행 후 사진도 확인.
- PASS: 사진 누락0, 원본UUID/revision/deleted/source관계/checksum보존, 재export 동일, 폰 device identity 유지.
- 실패 증적: 원본/복원본 ZIP과 해시·건수·복원 전후화면. 원복: 원본PC는 유지, 폰 시험 환경만 별도 정리.
- 상태: NOT RUN. 자동의 별도 Chrome Profile 복원은 실제 PC→폰 전송/파일 선택을 대신하지 않는다.

## DIET-MANUAL-07 — 손상 사진/ZIP 거부 / v1 호환

- 목적/사전조건: 정상 ZIP의 사본2개와 업데이트 전 정상 JSON v1, 기존 기록이 있는 **시험** 환경. 원본 백업을 수정하지 않는다.
- 순서: PC 탐색기로 ZIP 사본을 열어 media 파일1개를 제거하거나 파일 바이트를 변경한 사본을 준비. 설정 → 백업 복원 → 사본 선택.
- 예상: 사진 누락/손상 또는 지원되지 않는 ZIP 안내, 복원 실행 불가. 사진을 제외하고 부분 복원하는 선택지는 없어야 한다. 사본을 다시 압축하면서 STORE 형식이 바뀐 경우 형식 거부도 정상이다.
- 정상 ZIP을 다시 선택하면 미리보기 성공. 다른 pristine 시험 환경에 기존 JSON v1 선택→복원 → 원래 운동/체중/인바디/이용권 관계 보존, 사진0 정상.
- PASS: 손상 파일에서 전체 쓰기0, 정상ZIP 성공, 정상v1 계속 성공. 실패 증적: 오류코드·사본 조작방식·전후 data hash. 원복: 손상 사본만 폐기, 정상 백업 보존.
- 상태: NOT RUN.

## DIET-MANUAL-08 — 강제 v2 복원 / 백업 후 초기화 / 기존 회귀

- 목적/사전조건: 분리한 시험 환경, 사진 포함 백업 ZIP S 보관. 기존 회귀 MANUAL 절차 실행 가능한 시험 값 준비.
- 순서: 대상에 S와 다른 식단/사진을 추가 → 설정 → 백업 복원 → S 선택. 일반 복원은 차단. 강제 복원 → 현재 데이터를 먼저 백업하고 강제 복원 → 실제 내려받은 ZIP을 보관/재선택.
- 예상: 파일 검증 전 변경0, 확인 후 대상의 다른 사진까지 S로 교체. S와 portable/media checksum 일치, device_id 유지.
- 설정 → 전체 데이터 초기화 → 예·백업 후 초기화 → 내려받은 ZIP 다시 선택. 확인 후 초기 Profile/기본 운동, 식단/사진 파일0. 취소하면 아무것도 바뀌지 않는다.
- 기존 운동60→1회59→수정59→해제60, 예약생성60→완료59→중복59→완료취소60, 체중/인바디ON/OFF/복원, JSONv1 기능을 재확인한다.
- PASS: 강제교체/초기화 명시적 확인과 실제 파일 검증 유지, 사진 orphan0, 기존 업무회귀0.
- 실패 증적: 실행직전 두 ZIP, 전후hash/사진수/device_id, 취소/실행 버튼순서. 원복: 초기화한 **시험 환경**에 보관ZIP 일반복원. 운영환경에 무단적용하지 않는다.
- 상태: NOT RUN.

---

## 이전 버전 회귀 절차 (보존)

# v0.6.0 설치형 PWA 사용자 QA

HEALTH-MANUAL-01~08은 모두 **NOT RUN**. 기존 회귀 25건도 이 버전에서 자동 PASS로 승계하지 않는다. 아래 자동 Chrome 결과는 실제 Galaxy/Pages 검증을 대신하지 않는다. 이전 절차는 이 문서 하단에 보존했다. 기존 절차를 재실행할 때 앱 표시 버전은 0.6.0을 기대한다.

## 공통 사전조건 / 실패 증적 / 원복

1. 기존 설치 앱에서 설정 → 백업 내보내기. 다운로드 폴더에 실제 JSON이 저장됐는지 확인한다. 초기화/강제 복원 없이 일상 데이터와 분리한 시험용 브라우저에서 먼저 진행한다.
2. 실제 GitHub Pages 주소를 열고 홈 `v0.6.0 · DB 1`을 확인한다. 설정의 Schema1/14 Store/정상 상태 확인 후 Galaxy 설치형 PWA로 실행한다. 인터넷 연결 상태로 한 번 모든 화면을 연다.
3. 시험 날짜 **D는 실행 당일의 날짜**로 하고 입력 시 `D 08:00`처럼 실제 날짜를 선택한다. 고정 예: 2026-09-07 실행 시 D=2026-09-07. 기본 Profile 시간대는 Asia/Seoul. 시험값 앞 메모 `QA060`으로 구분한다.
4. 각 Test ID의 상태/시간, Galaxy 모델·Android/Chrome 버전, URL, standalone 여부, 네트워크를 기록한다. 실패 시 마지막 정상 단계, 실제 입력값, 예상/실제 값, 오류 문구, 전후 화면과 백업 JSON의 해당 UUID/revision/deleted_at/source/source_ref_id를 로컬에 보관한다.
5. 원복은 일반 체중은 기록 열기 → 삭제, 연동 체중은 **원본 인바디** → 삭제로 진행한다. 삭제 행은 soft-delete로 남는다. 시험용 환경만 비울 때 설정의 전체 초기화 절차를 따른다. 일상 데이터 환경은 시험 기록만 삭제하고 전체 초기화를 하지 않는다.

## HEALTH-MANUAL-01 — 일반 체중 CRUD / 같은 날 여러 측정

- 목적/사전조건: 빈 시험 환경, 위 공통 준비 완료.
- 순서/값: 체중 → + 체중 → D 08:00 / 70 kg / `QA060 아침` → 저장. `70 kg` 기록 상세가 보인다.
- 체중 목록 → + 체중 → D 20:00 / 69.5 / `QA060 저녁` → 저장 → 체중 목록. 최근 `69.5 kg`, 직전 대비 `-0.5 kg`, 그래프/측정값 보기 2건 예상.
- 아침 기록 열기 → 수정 → 70.2 → 저장. 목록 최근69.5/직전대비-0.7 예상. 저녁 기록 열기 → 삭제 → 확인. 상세 `삭제됨`, 목록 최근70.2로 바뀐다.
- 목록 → 삭제된 기록 포함 체크 → 저녁 기록 열기 → 복원. 최근69.5/-0.7과 2개 점 복귀.
- PASS: 같은 날짜 2건 UUID가 다르고 생성/수정/삭제/복원 흐름에서 위 값과 revision 증가가 정확하다.
- 실패 증적: 공통 + 각 시각/체중/변화량/조회 기간. 원복: 두 일반 체중 기록 삭제.
- 상태: NOT RUN.

## HEALTH-MANUAL-02 — 인바디 선택값 / 입력 검증

- 목적/사전조건: 체중 탭, 시험 환경. 연동 OFF 상태.
- 순서/값: + 인바디 → D 09:00, weight69, 골격근30, 체지방량17, 체지방률24.6, BMI23.1, 내장지방7, 기초대사1520, 메모 `QA060 인바디` → 저장.
- 예상: 상세에 7값과 메모, `체중 연동 OFF` 표시. 일반 체중은 추가되지 않는다.
- + 인바디 → D 10:00, 모든 숫자 빈칸, 메모 `QA060 항목 없음`, 연동OFF → 저장. 인바디만 저장되고 그래프에 0으로 표시되지 않는다.
- 새 인바디에서 체지방률101 또는 골격근-1 입력 후 저장을 시도한다. 저장되지 않고 해당 입력 안내가 나타나야 한다. 연동ON인데 체중 빈칸도 차단. 수정값을 버리고 취소한다.
- PASS: 없는 항목 저장 허용, 음수/101%와 체중 없는 ON 차단, 메모 유지. 실패 증적: 문제 항목·빈칸/0 구분·안내문구. 원복: 만든 인바디만 삭제.
- 상태: NOT RUN.

## HEALTH-MANUAL-03 — 연동 생성·수정 / OFF→ON 재사용

- 목적/사전조건: 별도 인바디를 새로 만들며 다른 QA 값과 혼동하지 않도록 메모 사용.
- 순서/값: + 인바디 → D 11:00 / 체중68.8 / 골격근30.1 / 메모 `QA060 연결` → `이 체중을 체중 기록에도 추가` ON → 저장.
- 예상: 목록에 인바디와 `체중 · 인바디 연동`이 각각 보인다. 체중 그래프의 새 점은 **1개**. 설정 → 백업 내보내기로 연결 weight ID와 source_ref_id를 기록한다.
- 연결 체중의 연결 정보 → 원본 인바디 열기 → 체중68.6 / D 11:30 / 메모 `QA060 동기화` → 저장. 두 엔티티의 체중/시각/메모가 일치해야 한다.
- 원본 인바디 수정 → 연동OFF → 저장. 연결 체중은 일반 목록/그래프에서 사라지고 삭제된 기록 포함에서만 보인다.
- 원본 수정 → 연동ON → 저장 → 백업 내보내기. 이전과 **같은 weight UUID** 복원, 동일 source_ref_id 행은 1건, 값68.6 예상.
- PASS: 원본 안내, 동기화, OFF 삭제, ON 동일행 재사용 및 그래프 중복0. 실패 증적: 전후 두 JSON의 해당 행. 원복: 원본 인바디 삭제.
- 상태: NOT RUN.

## HEALTH-MANUAL-04 — 인바디 삭제·복원 / OFF 의도

- 목적/사전조건: MANUAL-03의 연결 인바디를 유지하거나 같은 값으로 다시 생성.
- 순서: 원본 인바디 기록 열기 → 삭제 → 확인. 인바디와 연결 체중 둘 다 활성 목록/그래프에서 사라진다.
- 목록 → 삭제된 기록 포함 → **인바디** 기록 열기 → 복원. 두 기록 모두 동일 ID로 복귀하며 weight/source_ref_id 일치.
- 원본 수정 → 연동OFF → 저장 → 원본 삭제 → 복원. 인바디만 복귀하고 연결 체중은 삭제 상태 유지. 다시 ON으로 저장하면 기존 체중 행 복귀.
- PASS: ON 삭제/복원은 쌍, OFF 삭제/복원은 원본만 복원. 연결 체중에는 독립 수정·삭제·복원 버튼이 없어야 한다.
- 실패 증적: 연동 상태/두 삭제 시각/ID/그래프 점수. 원복: 마지막 원본 인바디 삭제.
- 상태: NOT RUN.

## HEALTH-MANUAL-05 — 기간·7개 그래프 / 캘린더·홈

- 목적/사전조건: 새 시험 환경 권장. 일반 체중 D08:00=70, D20:00=69.5; 연동 인바디 D21:00=69/골격근30/지방17/지방률24.6/BMI23.1/내장7/기초1520 생성.
- 순서: 체중 → 기간 7일/30일/3개월/전체 각각 선택. 당일 체중 **3점**. 측정값 보기에서 시각순 70→69.5→69 확인. 다른 6지표 선택 시 각 1점, 값 일치.
- D에서 10일 전 체중72를 추가한다. 7일은3점, 30일은4점. 생성 순서와 무관하게 최근은D21:00의69/직전대비-0.5 유지.
- 캘린더 → 오늘 → 해당 날짜. 체중2항목 + `인바디 · 체중 연동`1항목, 총3개 건강 항목(기존 운동은 별개). 측정 기록 열기가 원본으로 이동한다. 이전/다음 달 → 오늘 정상.
- 홈 → 최근 측정:69 kg/-0.5와 최근 인바디30/17 등 주요값·측정시각. 삭제/복원 후 다시 진입하면 요약 반영.
- PASS: 기간별 점수/값·시각순·관계 표시·원본 이동·최근 요약 정확. 실패 증적: 지표/기간/날짜/점수/화면. 원복: 만든 일반 체중3건과 원본 인바디1건 삭제.
- 상태: NOT RUN.

## HEALTH-MANUAL-06 — 비행기 모드 / 앱 종료 후 재실행

- 목적/사전조건: 온라인으로 v0.6.0 초기 로딩 완료, 설치형 PWA에서 측정2건 이상 준비.
- 순서: 비행기 모드 ON(Wi-Fi도 OFF) → 앱 완전 종료 → 설치 아이콘으로 재실행 → 체중. 기존 기록/그래프가 보인다.
- + 체중 → D22:00 / 68.9 / `QA060 오프라인` → 저장 → 수정68.7 → 삭제 → 복원. 같은 방식으로 + 인바디 D22:30 / 68.5 / 연동ON 저장, OFF→ON 실행.
- 지표/기간 전환, 캘린더 오늘, 홈 요약 확인. 다시 앱 종료→재실행 후 두 기록/연결 상태와 그래프 유지.
- PASS: 네트워크 없이 전체 흐름 성공, 재실행 후 UUID/값/연결 보존. 실패 증적: 비행기/Wi-Fi 설정과 재실행 화면, 실패 시각. 원복: 네트워크 복구 후 시험 일반 체중/원본 인바디 삭제.
- 상태: NOT RUN.

## HEALTH-MANUAL-07 — 백업 / pristine 복원

- 목적/사전조건: 일반 체중, ON 인바디, OFF 후 삭제된 연결 체중, 삭제된 원본 인바디를 포함하는 시험 환경. **원본 환경을 초기화하지 않는다.**
- 순서: 설정 → 백업 내보내기 → 실제 JSON 파일 보관. 별도 브라우저의 동일 Pages 앱을 처음 실행해 초기 Profile/기본 운동만 있는 pristine 상태 확인.
- 설정 → 백업 파일 선택 → 파일 선택 → 미리보기의 weight_logs/inbody_logs 건수 확인 → 복원 실행. 원본 Profile ID·측정값·메모·삭제 상태가 복귀한다.
- 복원된 환경에서 다시 백업. 두 JSON의 data.weight_logs와 data.inbody_logs에서 UUID/revision/created_at/updated_at/deleted_at/연결 source/source_ref_id/link_weight와 모든 측정값 일치 확인. exportedAt은 달라도 정상이며 integrity.payloadHash는 같아야 한다.
- 복원 환경의 삭제된 인바디 복원과 OFF→ON을 실행해 정상 동작 확인. populated 상태에서 일반 복원 버튼은 계속 차단되어야 한다.
- PASS: pristine 복원 성공, metadata/관계/hash 보존, 복원 후 CRUD 정상. 실패 증적: 두 JSON을 로컬 보관, 미리보기/오류 코드. 원복: 원본은 그대로 유지하고 시험 복원 환경만 정리.
- 상태: NOT RUN.

## HEALTH-MANUAL-08 — 기존 기능 / 업데이트 / 충돌

- 목적/사전조건: 이전 검증 앱 데이터 백업, v0.6.0 업데이트 설치. 같은 Profile을 두 창에서 연다.
- 순서: 이전 운동/Template v1/v2/메모/삭제 상태/이용권 잔여/예약이 유지되는지 확인. 기존 하단 QA051 및 v0.5 업무 절차를 재실행한다.
- 두 창에서 같은 인바디 수정 화면 열기 → A에서 체중68 저장 → B에서67 저장. B는 충돌 안내, 저장 실패. B를 새로 열면68이고 연결 체중도68.
- 입력 중 하단 다른 탭 선택 → 변경 버리기 취소 시 입력 유지, 확인 시 이동. 저장 버튼을 빠르게 두 번 누르면 단일 신규 기록만 저장.
- PASS: 기존 기능 회귀0, stale 변경0, 중복 제출0, 업데이트 후 데이터 보존. 실패 증적: 두 창 revision/오류/최종값과 기존 기능 테스트 ID. 원복: 시험값을 원본 화면에서 수정하거나 시험 기록 삭제.
- 상태: NOT RUN.

---

## 이전 버전 회귀 절차 (보존)

# v0.5.1 실제 GitHub Pages / Galaxy 설치형 PWA QA

이번 버전의 실기기 항목은 모두 **NOT RUN**이다. 자동 Chrome 검증과 구분한다. 기존 17개 배포/업무 회귀 항목에 아래 8개(QA051-MANUAL-01~08)를 추가하여 총 25개를 기록한다.

## 공통 사전조건 및 증적

실제 Pages HTTPS 하위 경로에서 홈 v0.5.1 / DB1 확인. Galaxy 모델·Android/Chrome 버전·실행 모드(탭/standalone)·URL·네트워크 상태를 기록한다. 초기화/강제 복원 테스트는 **운영 데이터와 분리한 브라우저**에서 수행한다. 기존 데이터는 설정 → 백업 내보내기로 보관하고, 새 브라우저에서 일반 복원하여 재현용 데이터로 만든다. 초기화/교체는 모든 Profile의 portable 기록을 대상으로 하며 device_id와 기기 설정은 유지한다.

테스트 실패 시 공통 기록: Test ID, 정확한 버튼 순서/입력값, 실패 직전/직후 화면, 오류 메시지/코드, 잔여횟수·예약상태, 앱 버전과 시각. 가능하면 전후 JSON을 로컬에 보관하고 ID/revision/deleted_at 및 integrity.payloadHash를 비교한다. 실제 건강 데이터 백업을 공개 저장소에 올리지 않는다.

## QA051-MANUAL-01 — 예약 기본 이용권 (자동 매핑 QA051-01/02)

- 사전조건: 운동 → 운동 관리 → + 새 운동 → `QA051 필라테스`, 운동시간 필드 → 저장.
- 순서/값: 운동 → 이용권 관리 → + 이용권 → QA051 필라테스 / `QA051 만료` / 60 / 2000-01-01~2000-12-31 / 활성 저장. 같은 운동의 `QA051 A` / 60 / 2026-01-01~2026-12-31 저장. 이어 `QA051 B`를 동일 조건으로 저장.
- 캘린더 → + 예약 → QA051 필라테스 / 2026-09-07 10:00 / 50분 저장. 2026년 9월로 이동 → 7일 → 예약 열기 → 예약 완료.
- 예상: 만료권은 후보에서 제외되고 생성순 첫 사용 가능권 A가 기본 선택. `차감 없음` 또는 B로 직접 변경 가능. A 선택 저장 시 잔여59, 차감 없음이면60.
- 이용권 관리에서 A/B 비활성화 후 별도 예정 예약 완료 화면을 열면 차감 없음이 기본값.
- PASS: 만료/비활성/잔여0 후보 기본 선택 없음, 유효 첫 항목 선택과 사용자 재선택 정상. 날짜 변경 후 최종 저장 검증은 실제 운동일 기준 유지.
- 원복: 완료 예약은 완료 취소 후 예약 취소. A/B 활성으로 돌리거나 시험용 비활성 상태로 둔다.
- 상태: NOT RUN. 실패 정보는 공통 증적 + 옵션 표시 순서/선택값/운동일.

## QA051-MANUAL-02 — 월간 달력 및 통합 표시 (QA051-03~06)

- 사전조건: 오늘 날짜의 예정 예약1건, 완료 예약1건, 직접 운동기록1건. 세 항목의 메모를 `예정/연결/직접`으로 구분한다.
- 순서: 캘린더 첫 진입 → 오늘이 선택된 월간 달력 확인 → 다른 날짜 클릭 → 이전 달 → 다음 달 → 오늘 버튼.
- 예상: 오늘 밑줄/테두리와 선택일 채움이 구분됨. 누른 날짜 데이터만 하단 표시. 완료 예약과 연결 기록은 `QA051 필라테스 · 완료 · 운동기록` 한 항목에 예약 열기/기록 열기 버튼. 직접 기록은 별도1건. 원본 데이터를 합치거나 삭제하지 않음.
- 경계값: 예정2026-08-31 10:00인 예약을 실제2026-09-01 10:00으로 완료. 8월31일에 완료 항목이 남지 않고 9월1일에 단일 항목 표시.
- PASS: 기본오늘/월이동/날짜필터/연결중복제거/직접기록 분리 모두 일치, 가로 넘침 없음.
- 원복: 시험 완료는 완료 취소, 예약 취소; 직접 기록 삭제.
- 상태: NOT RUN. 실패 정보: 표시월/선택일/실제일·예정일, 중복 항목 수와 연결 UUID.

## QA051-MANUAL-03 — 운동 종류 변경 중 입력 보존 (QA051-07~09)

- 사전조건: `QA051 운동A`는 운동시간/거리, `QA051 운동B`는 운동시간만 갖도록 운동 관리 → 양식에서 설정.
- 순서: 운동 → + 운동 기록 → A 선택 → 날짜2026-09-07 09:23 / 운동시간37 / 거리5 / 메모`전환 유지` 입력 → B 선택.
- 예상: 날짜/시간/메모/운동시간37 유지, 거리 입력 제거. A로 다시 바꿔도 이전에 제거된 거리값5가 부활하지 않음.
- 추가: 빠르게 A→B→A 전환 후 마지막 운동의 양식만 남고 저장 중 양식 혼용 없음. 같은 key지만 type 변경/허용 옵션 변경은 자동 테스트 fixture로 검증하며 실기기에서는 가능한 양식 변경 범위에서 확인한다.
- PASS: 공통 입력과 호환키 보존, 전용/비호환 값은 저장에 섞이지 않음.
- 원복: 입력 취소 → 미저장 이동 확인. 저장했다면 시험 기록 삭제.
- 상태: NOT RUN. 실패 정보: 전환 전후 field key/type 및 입력 스크린샷.

## QA051-MANUAL-04 — 상태 버튼과 원장 (QA051-10~12)

- 사전조건: A 이용권 사용1회, 총60/잔여59. 초기 JSON으로 pass revision과 usage 행을 기록.
- 순서: 운동 → 이용권 관리 → A의 비활성화 → 화면 활성화 버튼 확인 → 활성화.
- 예상: active에서 수정|비활성화, inactive에서 수정|활성화. 잔여59/사용1 유지, usage 행 내용 동일. pass revision만 각1 증가. deleted_at=null 유지.
- PASS: 버튼/상태/원장·잔여/ revision 규칙 모두 일치.
- 원복: 최초 상태로 돌리고 시험 기록 삭제(잔여60).
- 상태: NOT RUN. 실패 정보: 전후 상태/잔여량과 pass·usage revision.

## QA051-MANUAL-05 — 백업 후 전체 초기화 (QA051-13/14/16/22)

- 사전조건: 시험 브라우저에 기록·이용권·예약·삭제 이력. 기기 식별값은 원격 개발자 도구 사용 가능 시 device_settings.device_id를 읽기만 하여 기록.
- 순서: 설정 → 전체 데이터 초기화 → `초기화 전에 현재 데이터를 백업하시겠습니까?` → 예 · 백업 후 초기화.
- 예상1: JSON이 다운로드됨. **아직 데이터는 지워지지 않음**. 다운로드 파일 확인 입력 표시.
- 파일 앱에서 방금 내려받은 JSON 존재 확인 → `저장된 백업 파일 확인 후 실행`에서 그 파일 선택.
- 예상2: validator/hash 일치 후에만 초기화 → 앱 재실행. 기본 내 프로필/필라테스/Template1만 있고 운동기록·이용권·예약0. 설정 진단 정상/14 Store/DB1/Schema1/Seed1. device_id 동일.
- 실패 분기: 다운로드 취소 또는 파일 선택 취소 → 초기화 없음. 다른 정상 백업/손상 파일 선택 → 오류 및 초기화 없음. 백업 생성 실패(사진 미지원 등)는 초기화 금지. 검증 오류 뒤 취소 버튼으로 복귀.
- PASS: 백업 성공과 파일 재검증 전에는 삭제0, 성공 후 완전한 초기 상태. 자동 강제오류는 격리 테스트에만 주입한다.
- 원복: 초기화된 pristine 앱의 설정 → 백업 복원 → 저장 파일 선택 → 복원. 원본 hash/기록 확인.
- 상태: NOT RUN. 실패 정보: 다운로드 파일명/크기/hash, 파일선택 시각, 전후 건수/기기 ID.

## QA051-MANUAL-06 — 백업 없는 초기화 (QA051-15/16/22)

- 사전조건: 되돌릴 별도 JSON이 이미 있는 시험 브라우저. 운영 환경에서 수행하지 않음.
- 순서: 설정 → 전체 데이터 초기화 → 아니오 · 백업 없이 초기화 → 최종 확인에서 먼저 취소.
- 예상1: 기록 그대로. 다시 동일 순서로 최종 확인 승인.
- 예상2: 별도 다운로드 없이 초기 상태 생성, device_id 유지. 여러 Profile 데이터도 portable 범위에서 제거됨.
- PASS: 취소 시 변화0, 승인 시 부분 Seed/고아 기록 없이 정상 초기 상태.
- 원복: 사전에 보관한 백업으로 일반 복원.
- 상태: NOT RUN. 실패 정보: 최종 확인 승인 여부/전후 Profile·Store 건수.

## QA051-MANUAL-07 — non-pristine 강제 복원 (QA051-17~22)

- 사전조건: A 브라우저 백업 `원본.json`. B 시험 브라우저에 `교체될 기록`을 새로 만들어 non-pristine으로 준비.
- 순서: B 설정 → 백업 복원 → 원본.json → 미리보기. 일반 복원 비활성, 강제 복원 버튼 표시 확인.
- 강제 복원 → `현재 기록이 모두 백업 파일의 내용으로 교체됩니다.` 확인 → `현재 데이터를 먼저 백업하고 강제 복원` → 내려받은 B JSON을 재선택.
- 예상: B의 교체될 기록이 없어지고 원본 A의 UUID/profile_id/revision/created_at/updated_at/deleted_at/관계 그대로 복원. B device_id는 유지. B에서 다시 export한 integrity.payloadHash는 A 원본과 같음.
- 반복 시험: 시험용 기록 추가 후 파일 다시 선택 → 강제 복원 → 백업 없이 강제 복원 → 최종 승인. 결과 동일.
- 안전 분기: 준비/백업 확인 중 다른 탭에서 새 기록 저장 후 실행하면 대상 변경 오류로 중단. 다시 미리보고 새 백업을 만든 뒤 진행. 손상 checksum/schema/reference/unique 파일은 미리보기 단계에서 거절되며 강제 버튼 없음.
- PASS: 일반 차단 유지, 명시적 replace만 성공, merge 없음, 실패 시 기존 데이터 유지, hash/기기 ID 조건 충족. 중간 transaction 강제오류는 자동 검증에 한정.
- 원복: 백업한 B JSON을 강제 복원하여 B의 원상태로 복귀(일반 복원은 non-pristine 차단).
- 상태: NOT RUN. 실패 정보: 원본/보호 백업 hash·Profile UUID, 잔존·누락 ID, 오류 코드.

## QA051-MANUAL-08 — 오프라인·업데이트·기존 전체 회귀 (QA051-23/24)

- 사전조건: 실제 Pages 온라인 1회 실행/캐시 준비, v0.5.0에서 업데이트하여 v0.5.1 표시 확인, 보호 JSON 보관.
- 순서: Galaxy 비행기 모드 + Wi-Fi OFF → 설치형 앱 완전 종료 → 재실행. 월간 달력/기록 입력/이용권 상태 변경 실행. 시험 환경에서 05~07 백업·초기화·강제복원 흐름 반복.
- 예상: 오프라인 다운로드·파일 재검증·교체 및 재실행 성공. 기존 기록/Template/메모/soft-delete/revision 보존.
- 기존 차감 회귀: 60→운동1회59→메모수정59→차감해제60→재적용59→삭제60. 예약 생성/수정/취소60→완료59→중복완료59→완료취소60→재완료59.
- 기존 backup 일반 경로: 새 pristine 환경 정상 복원, populated 환경 일반 복원 차단. 삭제 기록 복원에서 inactive/기간/잔여량 재검증. 기존 5탭/진단/Template 버전 증가 유지.
- PASS: 모든 흐름 일치, 오프라인 재실행 후 상태 동일, 실제 사용자 기록 손실 없음. 사용자 실행한 범위만 PASS로 보고.
- 원복: 시험 완료 취소/예약 취소/일반 시험 기록 삭제, 또는 보호 JSON으로 강제 복원. 초기화/교체는 되돌리기 버튼이 없으므로 반드시 보관한 JSON을 사용한다. DB 자체 삭제로 문제를 해결하지 않는다.
- 상태: NOT RUN. 실패 정보: 네트워크/standalone 여부, 업데이트 전후버전, 실패 단계와 보호파일 존재.

## 기존 17개 수동 판정 항목

APP-001/002/003/004, CACHE-RUNTIME-001, BACKUP-QA-EXPORT/PREVIEW/RESTORE/OFFLINE, V05-QA-PASS/SCHEDULE/VALIDITY/LIMIT/CONFLICT/HISTORY/BACKUP/REGRESSION을 이번 버전에서도 NOT RUN으로 유지한다. 위 시나리오의 대응 부분과 기존 REGRESSION_TEST.md 기준으로 실제 재실행 후 개별 보고한다. 예전 PASS나 자동 테스트 결과로 승계하지 않는다.

보고 예: `QA051-MANUAL-07 / PASS / Galaxy 모델·Chrome / standalone / 보호파일 재선택 후 교체 / 원본 hash 일치 / 증적 파일명`.
