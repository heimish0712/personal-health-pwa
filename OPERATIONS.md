# 운영·복구 안내 — v0.9.0

APP/cache0.9.0 / DB2 / Schema1 / Seed1 / Backup2(JSON v1 호환). 이번 버전은 Migration 없음. 사용자 건강기록/사진은 기기의 IndexedDB에 있으며 앱 파일/cache와 별개다.

## 평상시 백업

설정 → 백업 내보내기 → 실제 파일 다운로드 확인 → 파일 선택으로 검증 성공 확인 → 기기 외 별도 위치에 보관한다. 사진이 있으면 ZIP v2, 없으면 JSON v1. 삭제 이력과 그 사진도 복원할 수 있도록 포함한다. 영구 저장 허용은 브라우저의 삭제 가능성을 줄이는 기능이며 백업을 대체하지 않는다.

## 복원

일반 복원은 초기 pristine 환경에서만 가능하다. 파일 선택/검증/미리보기 → 복원 → 재실행 → 데이터 진단 → 원본 기록·홈·캘린더·사진 확인. UUID/revision/삭제 이력/관계를 보존한다. 기존 데이터를 합치는 merge 기능은 없다. 강제 복원은 명시적 replace이며 모든 portable data와 media를 교체한다. 실행 전 현재 파일을 보관하고 미리보기/확인 내용을 읽는다.

손상 JSON/checksum, 누락·변조사진, 중복UUID/깨진관계, 미지원버전은 전체 거절한다. transaction 실패는 기존 상태로 rollback한다. 오류 화면 뒤에는 먼저 재실행/읽기 전용 진단으로 상태를 확인하고 자동 초기화하지 않는다.

## 초기화

시험 또는 명시적 사용자 의도일 때만 설정의 전체 초기화를 실행한다. `백업 후 초기화`는 export/self-validation 성공뿐 아니라 내려받은 파일의 재검증을 완료해야 진행된다. 실패하면 초기화 전 데이터와 device_id를 유지한다. 성공하면 device_id를 유지하고 초기 Profile/Seed를 새로 구성한다. 삭제 이력 일부를 정리하는 기능이 아니라 전체 reset이다.

## PWA 업데이트

온라인에서 새 버전 안내가 오면 작성 중 내용을 저장 → 업데이트 적용. 작성 중 적용을 누르면 기존 이탈 확인을 따른다. 다른 탭 등에서 새 worker가 활성화되어도 열린 폼은 자동 reload하지 않고 적용을 보류한다. 저장 후 업데이트를 적용한다. 적용 후 설정의 앱/DB/Schema와 데이터 진단을 확인한다. 업데이트 실패 시 열린 다른 탭을 닫고 연결/용량을 확인한 뒤 다시 시도하며 DB 삭제로 해결하지 않는다.

## 저장공간 부족

설정의 전체 사용량/quota는 Storage API origin 추정값으로 cache 등도 포함한다. 사진 사용량은 Blob.size 합계, portable 용량은 모든 Profile의 JSON UTF-8 추정치이며 IndexedDB 오버헤드는 별도다. API 미지원/거절은 값을 알 수 없는 상태다.

사진 저장 직전 usage+압축본/thumbnail 예상량이 quota90%에 이르면 경고한다. 취소하고 사진 크기/장수를 줄이거나 먼저 외부 백업을 보관한다. QuotaExceeded 시 기존 데이터는 유지된다. 기기 여유 공간도 확인한다. 사진 삭제는 soft-delete이므로 그 파일은 복원용으로 남아 즉시 공간이 줄지 않을 수 있다.

## 고아 파일·삭제 이력·로그

설정 → 고아 사진 파일 정리 → 예상 개수/용량 확인 → 명시적 확인. 실제 transaction에서 참조를 재검사하므로 미리보기 이후 상태가 바뀌면 삭제 개수가 달라질 수 있다. 활성 또는 삭제된 사진 metadata가 참조하는 파일은 모든 Profile에서 보존한다. 백업·복원·초기화 중에는 정리가 차단된다. Web Locks 미지원 환경은 GC를 실행하지 않는다.

portable tombstone 자동 purge/일반 사용자 purge는 제공하지 않는다. 향후 Sync의 삭제 이력으로 보존한다. app_logs만 최신200건으로 자동 제한하며 테스트 데이터 생성/정리는 이름이 격리된 테스트 DB에서만 수행한다.

## 데이터 진단과 로그

설정 → 데이터 진단 실행. 읽기 전용으로 연결 누락/중복 활성 원장/누락 사진/Profile pointer 등을 코드·건수로 표시한다. 자동 수정하지 않는다. 이 진단은 구조·관계 검사이며 사진 전체 해시 검증은 백업 검증에서 수행한다.

최근 진단 로그는50건을 시각/level/event와 고정 사용자 메시지로 보여준다. 상세는 code/name/version 등 허용된 진단 필드만 표시한다. `진단 로그 내보내기` 파일에는 건강정보 메모·사진 원문을 넣지 않는다. 외부 전달 전 사용자가 내용과 발생시각을 확인한다.

## 장애 발생 시 순서

1. 마지막 작업과 오류코드/시간/버전/선택 날짜를 기록한다. 실패한 저장이 성공했다고 가정하지 않는다.
2. 작성 중 미저장 값은 따로 보관하고 앱 재실행 후 원본 기록과 설정 진단을 읽는다.
3. 저장공간/네트워크/다른 탭을 확인한다. 업데이트는 사용자 적용 절차로 재시도한다.
4. 읽을 수 있는 데이터는 검증 가능한 백업으로 보관한다. 손상 진단이 있으면 강제로 덮어쓰지 않고 마지막 정상 백업을 별도 pristine 환경에서 검증한다.
5. 정상 백업의 원본·관계·사진이 확인된 후 사용자가 명시적으로 복원/replace 여부를 결정한다. 실패 원인을 DB 초기화로 숨기지 않는다.

## 운영상 한계

ZIP v2는 메모리에서 생성/검증하며 파일 크기·엔트리 제한이 있다. 대량 백업은 일시적으로 UI 작업 시간이 길어질 수 있다. portable 크기와 Storage API quota는 실제 브라우저 내부 파일 크기와 같지 않다. 관계 진단은 모든 portable row를 읽기 때문에 누적량에 비례한다. Web Locks와 Storage API 지원 여부는 브라우저마다 다르다. 자동 Chrome 성능 수치는 실제 Galaxy 측정을 대신하지 않는다. 지원 밖 파일은 데이터 초기화 대신 정상 버전/백업을 사용한다.
